import useContract from "../hooks/useContract";
// import {abi as ReportAbi,network as nETWO}

const reportRequest = ()=>{
    const params = useParams();
    const reportContract = useContract()

    return(
        <section>

        </section>
    )
}

export default reportRequest;